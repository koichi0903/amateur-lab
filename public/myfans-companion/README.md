# Myfans Companion

1. Chromeで `chrome://extensions` を開く。
2. デベロッパーモードを有効にする。
3. 「パッケージ化されていない拡張機能を読み込む」で `public/myfans-companion` を選ぶ。
4. amateur-lab を `npm run dev` などで起動する。
5. 通常Chromeでmyfansにログインし、商品ページまたはアフィリエイト画面を開く。
6. 拡張アイコンから「このページを候補登録」を押す。

この拡張はログイン情報を保存しません。Cloudflare回避や自動ログインは行いません。
